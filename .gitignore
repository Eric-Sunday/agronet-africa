# Dependencies
node_modules/

# Build output — Netlify builds this from source; don't commit it
dist/

# Environment variables (never commit secrets)
.env
.env.local
.env.production

# Editor / OS junk
.DS_Store
Thumbs.db
*.log

# Vite cache
.vite/
